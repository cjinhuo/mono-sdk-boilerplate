{
  "name": "@mono/core",
  "version": "0.0.1",
  "description": "@mono/core",
  "author": "cjinhuo",
  "license": "MIT",
  "main": "src/index.ts",
  "publishConfig": {
    "main": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "ts-node ./src/run.ts",
    "esm": "tsc -b ./tsconfig.json",
    "build": "rollup -c rollup.config.ts --bundleConfigAsCjs --configPlugin typescript",
    "build:js": "rollup -c rollup.config.js"
  },
  "dependencies": {
    "@mono/rollup": "workspace:^0.0.1",
    "@mono/shared": "workspace:^0.0.1",
    "@mono/tsconfig": "workspace:*"
  }
}